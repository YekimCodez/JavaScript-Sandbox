let insertItem = function(insertTask, submitButton, taskList) {
    const submitButton = document.querySelector("add"); // The button element
    const insertTask = document.querySelector("add_task"); // Get user input
    const taskList = document.querySelector("task_list"); // The ul element that hold the list

}

 