// If a person is on the compound
const loggedIn = false;
//If a person is not on the compound
const loggedOut = true;

let visitor;  

// If I wanted the program to state whether or not a person was on the compound.
let presentOnCompound = function() {
    return `Visitor left!! ${true}`;
}
console.log(presentOnCompound());


